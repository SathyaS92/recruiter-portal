import { JobUpdationDirective } from './job-updation.directive';

describe('JobUpdationDirective', () => {
  it('should create an instance', () => {
    const directive = new JobUpdationDirective();
    expect(directive).toBeTruthy();
  });
});
